# ADR（Architecture Decision Records）

設計判断を 1 件 1 ファイルで記録します。必ず要件へのリンクを含めます。

## ルール
- ファイル名: `YYYYMMDD-<短いタイトル>.md`
- 1 ADR = 1 判断
- 必ず `docs/requirements.md` の該当項目にリンクする

現行の判断記録は、要件とアーキテクチャの正本に集約しています。新しい判断記録を追加する場合は、このディレクトリに要件へのリンクを含む Markdown ファイルを作成してください。
